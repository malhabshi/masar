import { Card, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export default function DashboardPage() {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Welcome to your Dashboard</CardTitle>
          <CardDescription>
            This is your new, stable dashboard page. We can now start migrating your components here.
          </CardDescription>
        </CardHeader>
      </Card>
    </div>
  )
}
