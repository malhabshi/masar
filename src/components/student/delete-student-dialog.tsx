'use client';
import type { Student, User } from '@/lib/types';

export function DeleteStudentDialog({ student, currentUser }: { student: Student, currentUser: User }) {
    return <div>Delete Student Dialog Placeholder</div>;
}
