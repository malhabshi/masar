'use client';
import type { Student, User } from '@/lib/types';

export function IeltsSection({ student, currentUser }: { student: Student, currentUser: User }) {
    return <div>IELTS Section Placeholder</div>;
}
