'use client';
import type { Student, User } from '@/lib/types';

export function ProfileCompletionProgress({ student, currentUser }: { student: Student, currentUser: User }) {
    return <div>Profile Completion Placeholder</div>;
}
