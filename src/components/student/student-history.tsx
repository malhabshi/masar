'use client';
import type { Student, User } from '@/lib/types';

export function StudentHistory({ student, users }: { student: Student, users: User[] }) {
    return <div>Student History Placeholder</div>;
}
